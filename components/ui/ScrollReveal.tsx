'use client';

// Reveal animations are handled entirely by CSS keyframes (revealUp in globals.css).
// This component is kept as a placeholder for future scroll-based enhancements.
export default function ScrollReveal() {
  return null;
}
